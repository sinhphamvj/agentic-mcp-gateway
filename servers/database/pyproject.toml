[project]
name = "agentic-mcp-gateway-database-server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["mcp[cli]>=1.0", "starlette>=0.40", "uvicorn>=0.30"]

[project.scripts]
serve = "server:main"
